data = [
    ['Sunny', 'Warm', 'Yes'],
    ['Sunny', 'Cold', 'Yes'],
    ['Rainy', 'Warm', 'No']
]

h = data[0][:-1]

for row in data:
    if row[-1] == 'Yes':
        for i in range(len(h)):
            if h[i] != row[i]:
                h[i] = '?'

print("Hypothesis:", h)
